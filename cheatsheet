## xfce shortcuts:

 * ctrl-alt-< and> = move between workspaces
 * alt-f1 = apps menu
 * alt-f2 = launcher
 * alt-f4 = close app
 * alt-f11 = fullscreen toggle

## My xfce shortcuts:

 * ctrl-alt-t = xfce4-terminal --maximize --fullscreen
 * ctrl-alt-g = gvim -f
 * ctrl-alt-b = /usr/bin/google-chrome-stable %U

## My terminal aliases/functions:

 * execline n file = execute a line in a file in current bash session
 * acg n = git add all files and commit with message n
 * log n = git pretty 1 line log of n length (defaults to 5)

